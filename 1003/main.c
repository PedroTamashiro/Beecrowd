#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

int main(void){
    int A, B, Soma;
    scanf("%i", &A);
    scanf("%i", &B);
    Soma = A + B;
    printf("Soma = %i\n", Soma);
}